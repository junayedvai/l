This package was cleaned to help you push to GitHub again.

What was cleaned:
- removed .git history
- removed node_modules
- ensured .env.example contains no real token

Next steps:
1. Extract this zip
2. Open terminal in the project folder
3. Run:
   git init
   git add .
   git commit -m "Initial clean commit"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
   git push -u origin main

Before deploying on Vercel, add real environment variables in Vercel Project Settings > Environment Variables.
Do not put real tokens inside .env.example.
